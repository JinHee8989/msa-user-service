server:
  port: 9001

spring:
  application:
    name: user-service

eureka:
  client:
    register-with-eureka: true #eureka에 등록하겠다는 의미
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
