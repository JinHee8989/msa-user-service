server:
  port: 0 # 0은 랜덤포트를 사용하겠다는 뜻

spring:
  application:
    name: user-service



eureka:
  client:
    register-with-eureka: true #eureka에 등록하겠다는 의미
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
